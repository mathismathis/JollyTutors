/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['lh3.googleusercontent.com'], // Add the domain from which images are being served
    },
  };
  
  export default nextConfig;
  
